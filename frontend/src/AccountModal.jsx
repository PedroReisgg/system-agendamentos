import React,{useMemo,useState}from'react';
function strength(password){let score=0;if(password.length>=8)score++;if(/[A-Z]/.test(password)&&/[a-z]/.test(password))score++;if(/\d/.test(password)&&/[^A-Za-z\d]/.test(password))score++;return score;}
export default function AccountModal({mode,setMode,onClose,onLogin,onRegister,services,busy,error}){const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[phone,setPhone]=useState(''),[gender,setGender]=useState('OUTRO'),[favorites,setFavorites]=useState([]);const score=useMemo(()=>strength(password),[password]);const level=score<=1?['fraca','Fraca']:score===2?['media','Média']:['forte','Forte'];const toggle=id=>setFavorites(items=>items.includes(id)?items.filter(item=>item!==id):[...items,id]);return <div className="modal-backdrop"><section className="account-modal" role="dialog" aria-modal="true"><button className="close" onClick={onClose}>×</button><p className="eyebrow">ÁREA DO CLIENTE E EQUIPE</p><h2>{mode==='login'?'Acesse sua conta.':'Comece pelo seu cadastro.'}</h2><div className="account-tabs"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Entrar</button><button className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Criar conta</button></div>{mode==='login'?<form onSubmit={e=>{e.preventDefault();onLogin({email,password});}}><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength="3"/></label><button className="primary" disabled={busy}>{busy?'Entrando...':'Entrar'}</button></form>:<form onSubmit={e=>{e.preventDefault();onRegister({name,email,password,phone,gender,favoriteServices:favorites});}}><label>Nome completo<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength="8"/><span className={`password-strength ${level[0]}`}><i/><i/><i/><b>{password?`Senha ${level[1].toLowerCase()}`:'Use 8+ caracteres, maiúscula, número e símbolo.'}</b></span></label><label>WhatsApp<input type="tel" placeholder="+5511999999999" value={phone} onChange={e=>setPhone(e.target.value)} pattern="\+[1-9][0-9]{7,14}" required/></label><label>Gênero<select value={gender} onChange={e=>setGender(e.target.value)}><option value="M">Masculino</option><option value="F">Feminino</option><option value="OUTRO">Outro</option></select></label><fieldset><legend>O que você costuma agendar?</legend>{services.map(service=><label className="check" key={service.id}><input type="checkbox" checked={favorites.includes(service.id)} onChange={()=>toggle(service.id)}/>{service.name}</label>)}</fieldset><button className="primary" disabled={busy}>{busy?'Enviando...':'Criar e verificar conta'}</button></form>}{error&&<p className="form-error">{error}</p>}<p className="form-hint">Contas da equipe usam o mesmo formulário de entrada.</p></section></div>}
